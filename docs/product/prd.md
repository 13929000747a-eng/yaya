# Part 2 + Part 3 详细需求

## Part 2: Cue Card 描述题

### 功能需求
1. **题目展示**
   - 显示完整题目描述
   - 显示 4 个提示点
   - 显示题目分类和难度

2. **录音功能**
   - 点击麦克风开始录音
   - 2 分钟倒计时
   - 录音中闪烁动画
   - 点击停止录音
   - 支持提前结束

3. **评估流程**
   - 上传录音到服务器
   - 调用 iFlytek ISE 评估
   - 调用 DeepSeek AI 评分
   - 解析 AI 返回的 JSON 结果

4. **结果展示**
   - Overall Band Score
   - 四维评分（带进度条）
   - 详细反馈文本
   - 录音回放
   - "再练一次" 按钮
   - "继续 Part 3" 按钮

### UI 设计
```
┌─────────────────────────────────┐
│  📝 Part 2 练习                 │
├─────────────────────────────────┤
│  🎯 Cue Card                    │
│                                 │
│  Describe a person you know    │
│  who is good at their job       │
│                                 │
│  You should say:                │
│  • Who this person is           │
│  • What job they do             │
│  • How you know them            │
│  • Why they are good...         │
│                                 │
│  ⏱️ 准备时间：1 分钟            │
│  ⏱️ 答题时间：2 分钟            │
│                                 │
│       ┌─────────┐               │
│       │   🎤    │               │
│       └─────────┘               │
│                                 │
│       点击开始录音               │
└─────────────────────────────────┘
```

## Part 3: 深度讨论

### 功能需求
1. **题目展示**
   - 显示讨论题目
   - 显示关联的 Part 2 话题

2. **录音功能**
   - 3 分钟倒计时
   - 录音中显示波形（可选）
   - 实时转文字显示（可选）

3. **评估流程**
   - iFlytek ASR 转文字
   - DeepSeek AI 评估逻辑性

4. **结果展示**
   - 逻辑性评分
   - 深度评分
   - 详细反馈
   - 改进建议

### UI 设计
```
┌─────────────────────────────────┐
│  💬 Part 3 练习                 │
├─────────────────────────────────┤
│  📌 关联话题                     │
│  Describe a person...           │
├─────────────────────────────────┤
│  What qualities do you think    │
│  are important for someone      │
│  to be good at their job?       │
│                                 │
│  ⏱️ 答题时间：3 分钟             │
│                                 │
│       ┌─────────┐               │
│       │   🎤    │               │
│       └─────────┘               │
│                                 │
│       点击开始录音               │
└─────────────────────────────────┘
```

## 技术实现要点

### 录音管理
```typescript
// 使用 MediaRecorder API
const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
const mediaRecorder = new MediaRecorder(stream);

// 存储录音片段
mediaRecorder.ondataavailable = (event) => {
  chunks.push(event.data);
};

// 结束后合并
const audioBlob = new Blob(chunks, { type: 'audio/webm' });
```

### API 调用
```typescript
// Part 2 评估
const result = await evaluatePart2(transcript, topic, hints);

// Part 3 评估
const result = await evaluatePart3(transcript, question);
```

### 状态管理
```typescript
// Part 2 状态
interface Part2State {
  isRecording: boolean;
  remainingTime: number;
  isEvaluating: boolean;
  result: Part2Result | null;
}
```

## 验收标准

1. ✅ Part 2 练习页可正常练习
2. ✅ Part 3 练习页可正常练习
3. ✅ 录音功能正常（2分钟/3分钟倒计时）
4. ✅ AI 评估正常（ISE + DeepSeek）
5. ✅ 结果展示清晰（Band + 四维评分 + 反馈）
6. ✅ 响应式设计，适配移动端
7. ✅ 麦克风权限处理
8. ✅ 录音文件正确上传
